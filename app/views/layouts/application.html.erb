<!DOCTYPE html>
<html>
  <head>
    <title>HealLine : Suivez votre parcours santé</title>
    <meta name="description" content="<%= meta_description %>">

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Ruluko&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400&display=swap" rel="stylesheet">
    <link rel="icon" type="logo" href="<%= image_url("caduceus.png") %>">
    
    <%= favicon_link_tag %>

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="<%= meta_title %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= meta_image %>" />
    <meta property="og:description" content="<%= meta_description %>" />
    <meta property="og:site_name" content="<%= meta_title %>" />

    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
    <link rel="stylesheet" href="https://unpkg.com/swiper/swiper-bundle.min.css" />

    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>

  <body id="<%= controller.controller_name %>-<%= controller.action_name %>">
    <% unless controller.action_name == "landing" %>
      <%= render 'shared/navbar' %>
    <% end %>
    <%#= render 'shared/flashes' %>
    <div class="<%= action_name == 'timeline' || action_name == 'new' || action_name == 'edit'  || action_name == 'index' || action_name == 'profile' ? 'container-timeline' : 'container-custom' %>">
      <%= yield %>
    </div>
    <%= render 'shared/flashes' %>
    <% unless controller.action_name == "landing" %>
      <%= render 'shared/footer' %>
    <% end %>
  </body>
</html>
